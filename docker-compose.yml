services:
  postgres:
    image: postgres:16
    container_name: insta_nest
    environment:
      POSTGRES_USER: andrew
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: instadb
    ports:
      - '5434:5432'
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U postgres -d tasks']
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

volumes:
  postgres_data: